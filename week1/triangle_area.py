# Write the function triangleArea(base, height) that takes two integers, base and height, 
# and returns the area of a triangle whose base and height are equal to those values.

# Hint: The formula for the area of a triangle is (base * height) / 2.

def triangleArea(base, height):
    return base * height / 2
    print("hello")
    
print(triangleArea(4, 5))